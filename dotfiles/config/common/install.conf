home/bin,,dir,~/bin

# darwin packages
wget,darwin,brew,wget
tree,darwin,brew,wget
lazygit,darwin,brew,lazygit
direnv,darwin,brew,direnv
ag,darwin,brew,the_silver_searcher
rg,darwin,brew,ripgrep
delta,darwin,brew,git-delta
jq,darwin,brew,jq
tfenv,darwin,brew,tfenv
k9s,darwin,cmd,brew install derailed/k9s/k9s

starship,darwin,brew,starship
starship,linux,cmd,cargo install starship
bat,,cmd,cargo install bat

fnm,,cmd,curl -fsSL https://fnm.vercel.app/install | bash -s -- --install-dir "~/.fnm" --skip-shell

# linux apt packages
wget,linux,apt,wget
ag,linux,apt,silver-searcher
gcc,linux,apt,build-essential
curl,linux,apt,curl
pip3,linux,apt,python3-pip
xclip,linux,apt,xclip
netstat,linux,apt,net-tools
docker,linux,apt,docker.io

# tmux
~/.tmux/plugins/tpm,,cmd,git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm
#tmux,,brew,tmux
#reattach-to-user-namespace,darwin,brew,reattach-to-user-namespace
# https://github.com/pangliang/oh-my-tmux
#~/.config/oh-my-tmux,,cmd,git clone https://github.com/gpakosz/.tmux.git ~/.config/oh-my-tmux
#~/.tmux.conf,,cmd,ln -s ~/.config/oh-my-tmux/.tmux.conf ~/.tmux.conf

rustup,,cmd,curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
